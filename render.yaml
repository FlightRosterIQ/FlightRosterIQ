services:
  - type: web
    name: flightroster-backend
    runtime: node
    rootDir: backend
    buildCommand: bash render-build.sh
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
